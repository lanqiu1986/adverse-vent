<div class="col-md-12">
  <div class="event">
    <%= link_to("Edit", edit_event_path(@event), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @event.title %></h2>
  <p><%= @event.department %></p>
  <p><%= @event.description %></p>
</div>
